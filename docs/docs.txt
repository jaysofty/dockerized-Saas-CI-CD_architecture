Architecture

                     Next.js Frontend
                           │
                 API Service Layer (fetch)
                           │
                  Express REST API (Docker)
                           │
                  Prisma ORM (Docker)
                           │
                     PostgreSQL (Docker)


These are the core technologies used to build and deploy FormFlow. 
The application is containerized with Docker, 
uses PostgreSQL for persistence, 
Terraform for infrastructure provisioning, 
and GitHub Actions for continuous integration and deployment."


final production Architecture

                 Internet
                     │
             Public IP (80/443)
                     │
                 Ubuntu VM
                     │
          Docker Compose Stack
        ┌──────────┼──────────┐
        │          │          │
     Nginx     Frontend    Backend
                               │
                          PostgreSQL
                     (Docker Container)
                     │
             Azure Bastion (Management)
                     │
               Azure Portal SSH
--------------------------------------------------------------------

✅ Phase 1
Application Development
Docker
Docker Compose
Terraform

────────────────────────────

▶ Phase 2
Docker Hub Release Management

Step 1
Version Strategy

Step 2
Build Images

Step 3
Tag Images

Step 4
Push Images

Step 5
Verify Images

Current Progress

✅ Stable main branch
✅ dev branch for experimentation
✅ Local compose working
✅ Production compose using Docker Hub images
✅ Versioned images (1.0.0)
✅ Docker Hub repositories created
✅ Images successfully built and pushed


────────────────────────────

Phase 3

CI Workflow Built

Instead of manually typing Docker commands, 
GitHub Actions will do this automatically whenever a version tag is created.


Developer

git tag v1.0.1

git push origin v1.0.1

        │

        ▼

GitHub Actions

        │

Checkout Repository

        │

Login Docker Hub

        │

Build Backend

        │

Build Frontend

        │

Build Nginx

        │

Push

backend:1.0.1

frontend:1.0.1

nginx:1.0.1

        │

Done


The CD Workflow

Once a VM (Azure, AWS, Oracle Cloud, etc.), a second workflow will:

SSH into VM

↓

git pull

↓

docker compose pull

↓

docker compose up -d

↓

Health Check

↓

Deployment Complete
The only change needed later will be adding the VM's SSH credentials as GitHub Secrets.


────────────────────────────

Phase 4
Azure VM Deployment

────────────────────────────


----------------------- completed Phase 1 ----------------
Current Progress
✅ Express + TypeScript
✅ PostgreSQL
✅ Prisma
✅ Dockerized backend
✅ Docker Compose orchestration
✅ Swagger/OpenAPI grants users a UI for API test
✅ React Query
✅ Next.js frontend communicating with the backend
✅ Live dashboard statistics
✅ Health endpoint
✅ Professional landing page sections
✅ Nginx Reverse Proxy: port 80


-----------------------------completed phase 2---------------------------

✅ Backend containerized
✅ Frontend containerized
✅ Nginx containerized
✅ PostgreSQL containerized
✅ Multi-stage Dockerfiles
✅ Docker Compose (development)
✅ Docker Compose (production)
✅ Images tagged
✅ Images pushed to Docker Hub
✅ Automatic Prisma migrations
✅ Git branching strategy (main/dev)
✅ Production deployment tested locally using Docker Hub images



----------------------------------- Next phase 3 --------------------------------

CI/CD pipeline .

Developer
     │
     ▼
Push to dev
     │
     ▼
GitHub Actions
     │
     ├──────────────
     │ Checkout
     │
     ├──────────────
     │ Build backend image
     │
     ├──────────────
     │ Build frontend image
     │
     ├──────────────
     │ Build nginx image
     │
     ├──────────────
     │ Login DockerHub
     │
     ├──────────────
     │ Push images
     │
     └──────────────
          │
          ▼

          Later...

Push to main
        │
        ▼
GitHub Actions
        │
        ▼
SSH Azure VM
        │
        ▼
docker compose pull
docker compose up -d

---------------------------------------------
create a repo and push to a branch

Git log showing the commits:
git log --oneline

tag a release.
git add .
git commit -m "feat(api): complete TypeScript backend with Prisma"
git tag v0.1.0
git push origin main --tags
---------------------------------------

Create the development branch
create it from the latest main.

git checkout main
git pull origin main
git checkout -b dev
git push -u origin dev
----------------------------------------------

— Development workflow

When working:

git checkout dev

Never develop directly on main.

When a feature works:
git add .
git commit -m "feat(ci): add github actions pipeline"
git push origin dev

When satisfied:

git checkout main
git pull origin main
git merge dev
git push origin main


if you mistakenly worked on main branch you can stash the changes 
checkout of main to dev and pop the changes to dev
example:

git stash push -u -m "Terraform refactor"
git status
git checkout dev
git stash pop
git add .
git commit -m "Refactor Terraform project structure"
git push origin dev

--------------------------------------------


Backend Development
Project initialization

Commands
npm init
npm install

installed packagesExpress setup
Controllers
Routes
Middleware
Validation
Error handling
Swagger
Health endpoint

frontend development
frontend package installations in requirements.txt


Database
PostgreSQL

Installing Prisma
Commands
npx prisma init
npx prisma migrate dev
npx prisma studio
npx prisma generate


Docker
Dockerfile
Docker Compose
Images
Containers
Volumes
Networks

Local build
build frontend docker image:
docker build -t formflow-frontend:dev .

build backend image:
docker build -t formflow-backend:dev .

or 

docker build \
  -t formflow-backend:dev \
  -f backend/Dockerfile \
  ./backend

nginx build:
docker build -t formflow-nginx:dev .

Tag images manually
docker tag formflow-backend:dev kunzydev/formflow-backend:1.0.0

docker tag formflow-frontend:dev kunzydev/formflow-frontend:1.0.1

docker tag formflow-nginx:dev kunzydev/formflow-nginx:1.0.0

Useful Docker Compose Commands for troubleshooting and deployments:
cd to deployment directory or root dir

start postgres:
docker compose up -d postgres

apply prisma migration:
docker compose run --rm backend npx prisma migrate deploy

Local development
docker compose up --build or docker compose up -d


check prisma migration
docker exec -it formflow-backend npx prisma migrate status


Production image build and push:
docker-compose.prod.yml:

verify the validity of docker compose file:
docker compose --env-file .env.production -f docker-compose.prod.yml config

create a manual production release build with tags for docker hub:
Backend
docker build \
-t kunzydev/formflow-backend:${IMAGE_TAG} \
-f backend/Dockerfile \
./backend

Frontend
docker build \
-t kunzydev/formflow-frontend:${IMAGE_TAG} \
-f frontend/Dockerfile \
./frontend

or 

docker build \
  -t kunzydev/formflow-frontend:1.0.3 \
  -f frontend/Dockerfile \
  ./frontend

Nginx
docker build \
-t kunzydev/formflow-nginx:${IMAGE_TAG} \
-f deployment/nginx/Dockerfile \
./deployment/nginx


if IMAGW_TAG command doesnt respond load env to shell terminal
set -a
source .env.production
set +a


push to docker hub:

docker push kunzydev/formflow-backend:${IMAGE_TAG}

docker push kunzydev/formflow-frontend:${IMAGE_TAG}

docker push kunzydev/formflow-nginx:${IMAGE_TAG}


test production compose.yml:

pull before push

docker compose \
  --env-file .env.production \
  -f docker-compose.prod.yml \
  pull


docker compose \
--env-file .env.production \
-f docker-compose.prod.yml \
up -d


docker compose \
  --env-file .env.production \
  -f docker-compose.prod.yml \
  stop

individual stop
docker stop formflow-nginx
docker stop formflow-frontend
docker stop formflow-backend
docker stop formflow-postgres

git checkout main
git merge dev

git tag v1.0.3
git push origin main
git push origin v1.0.3

for every new image push include github tags
--------------------------------------

The workflow:
docker compose -f docker-compose.prod.yml --env-file .env.production build

followed by pushing to docker hub:
docker compose -f docker-compose.prod.yml --env-file .env.production push

Uses:
docker-compose.prod.yml

start all existing containers:
docker compose up -d

Open:
http://localhost

Stop all services:
docker compose down

View logs from all services:
docker compose logs -f

View logs for the frontend:
docker logs formflow-frontend --tail=100

View logs for the backend:
docker logs formflow-backend --tail=100

check frontend container:
docker exec formflow-frontend printenv | grep PORT

check what version of image is running
docker inspect formflow-backend --format='{{.Config.Image}}'

Stop and remove volumes (this deletes your PostgreSQL data)
docker compose down -v
Rebuild after code changes

If you change the Dockerfile, package dependencies, or build output:
docker compose up --build -d

confirm docker network
docker network ls

debug prisma table
docker exec -it formflow-postgres psql -U postgres -d formflow

then:
\dt

check prisma migrate status
docker exec -it formflow-backend npx prisma migrate status

check prisma schema inside container:
docker exec formflow-backend cat prisma/schema.prisma

deploy prisma migration if needed:
docker exec -it formflow-backend npx prisma migrate deploy

verify tables:
docker exec -it formflow-postgres psql -U postgres -d formflow

\dt


--------------------- Next phase: Github actions CI/CD workflow -------------------------
GitHub Secrets:

Go to

GitHub

Repository

Settings

Secrets and Variables

Actions

Create these:

DockerHub
DOCKER_USERNAME

value: kunzydev or your docker hub username


DOCKER_PASSWORD

value:
(Docker Hub Access Token)

Generate one from Docker Hub

Account

Security

Access Tokens

when VM is up and running ill add

VM_HOST

VM_USER

VM_SSH_KEY


create dir:
.github/workflows/docker-build.yml

this docker build will
do this:

Push to main

↓

Build backend

↓

Build frontend

↓

Build nginx

↓

Push to Docker Hub

the workflow will essentially execute the same commands as the production compose.yml:
docker build \
-t kunzydev/formflow-backend:${IMAGE_TAG} \
-f backend/Dockerfile \
./backend

push to docker hub:
docker push kunzydev/formflow-backend:${IMAGE_TAG}


Once Azure is available, we'll add another job:
deploy:

that will

SSH into the VM and run

---------------------------------completed phase 3 ---------------------------------

✅ Git branching strategy (main + dev)
✅ Local Docker development
✅ Production compose file
✅ Versioned Docker images
✅ Automatic Prisma migrations
✅ Docker Hub publishing
✅ GitHub Actions building successfully

Push to dev
      │
      ▼
GitHub Actions
      │
      ▼
Build backend
Build frontend
Build nginx
      │
      ▼
Push tagged images to Docker Hub
----------
The only missing piece:

GitHub Actions
      │
      ▼
SSH into Azure VM
      │
      ▼
docker compose pull
docker compose up -d

------------------------------------ Next Phase Terraform automation for  azure resources -------------------------

The Full Module Plan

✅ Resource Group
🔜 Virtual Network
🔜 Subnet
🔜 Network Security Group
🔜 Public IP
🔜 Network Interface
🔜 Linux Virtual Machine
🔜 Managed Identity (optional but recommended)
🔜 Monitoring
🔜 Outputs

✅ Current Infrastructure
✔ Resource Group
✔ Virtual Network
✔ Application Subnet
✔ Azure Bastion Subnet
✔ Network Security Group
✔ NSG Association
✔ SSH Rule
✔ HTTP Rule
✔ HTTPS Rule
✔ Public IP
✔ Network Interface
✔ Bastion Public IP
✔ Azure Bastion Host
🔜 VM


Test the terraform Modules
From the terraform directory:
cd terraform/environments/dev
RUN:
terraform init

terraform validate

terraform fmt

terraform plan

terraform apply

terraform state list


after resources applied 

get vm publicIp:
az vm list -d -o table

SSH into the VM:
ssh azureuser@4.168.233.3

If everything is configured correctly, you'll get a shell like:
azureuser@formflow-dev-vm:~$

Verify cloud-init:
docker --version

check git version:
git --version

check whether your repository was cloned:
ls /opt

cd /opt/formflow
ls

check running container 
docker ps -a

docker compose up -d

check logs
sudo tail -100 /var/log/cloud-init-output.log

get ssh key
ls ~/.ssh

view ssh key:
cat ~/.ssh/id_ed25519

show what key terraform vm is using
cat terraform/environments/dev/terraform.tfvars

ssh to vm and create deployment directory
sudo mkdir -p /opt/formflow/deployment
sudo chown -R azureuser:azureuser /opt/formflow
-------------------------------------------------Remaining tasks:

✅ Create GitHub Actions workflow to build and push Docker images.
⏳ Add automated deployment over SSH to the Azure VM.
⏳ Resolve Azure VM SKU/capacity issue (or document it if required by the project).
⏳ Write the documentation sections covering:
Image tagging strategy.
Rollback procedure.
Secrets handling plan.
CI/CD architecture.
Azure deployment process.

Required GitHub Secrets

When your Azure VM is available, add these repository secrets:
AZURE_VM_IP	20.xx.xx.xx
AZURE_VM_USER	azureuser
AZURE_VM_SSH_KEY	Entire private SSH key

How the pipeline works
Every push to the dev branch will:

Build Backend
Push Backend to Docker Hub
Build Frontend
Push Frontend
Build Nginx
Push Nginx
SSH into Azure VM
Pull the new tagged images
Restart containers
Remove unused images

================= Errors encountered during GitActions Workflow build ===========

Authentication issue: didn't include the right secret for password 
Backend Package.lock.json issue: .gitignore excluded my package.lock.json file and git couldn't track it 

checked with
git check-ignore -v backend/package-lock.json

resolved with: removing it from .gitignore

Backend Package.lock.json issue: git wasn't tracking it

resolved with: git add -f frontend/package-lock.json
git status


CI/CD
the only major piece left is the SSH deployment job to the Azure VM

encountered a VM provisioning limitation error

Azure SKU/region capacity prevented full deployment
-------------------------------------------------------------------

Final Architecture
Developer
     │
git push dev
     │
     ▼
GitHub Actions
     │
     ├──────────────┐
     │              │
     ▼              ▼
Build Images   Push Images
     │              │
     └──────┬───────┘
            │
            ▼
SSH to Azure VM
            │
            ▼
docker compose pull
            │
            ▼
docker compose up -d
            │
            ▼
Users access
http://<vm-public-ip>